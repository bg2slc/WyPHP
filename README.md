Basic PHP Template Project, with the goal of being javascript free.
So far, very basic, index file serves as main page, lib file with helper functions, and styesheet.php which gets served as a css file.
